---
  web_containers:
    - traefik
    - prometheus
    - registry
    - portus
  portus:
    domain: "portus.{{ topleveldomain }}"
  registry:
    domain: "registry.{{ topleveldomain }}"
    port: 5000
  prometheus:
    domain: "metrics.{{ topleveldomain }}"